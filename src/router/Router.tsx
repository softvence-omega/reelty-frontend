import { createBrowserRouter } from "react-router";

const router = createBrowserRouter([
    {
        path: "/",
        element: <div className="bg-red-200 text-white">Hello world</div>
    }
])


export default router;